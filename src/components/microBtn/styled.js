import { StyleSheet } from 'react-native';
import { Colors } from '../../shared/tokens';

export default styles = StyleSheet.create({
 
    microBtn: {
        backgroundColor: Colors.yandexLightGreen,
        width: 48,
        height: 48,
        borderRadius: 56,
        justifyContent: 'center',
        alignItems: 'center',
  },
 
});
